# Encoding-and-Decoding-for-passwords
A simple java swt project; which when executed can provide UI to encrypt &amp; decrypt passwords. The project can be exported as runnable jar to create as utility.
